apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: favourite-service-hpa
  namespace: {{ .Values.global.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: favourite-service
  minReplicas: {{ index .Values "favourite-service" "hpa" "minReplicas" }}
  maxReplicas: {{ index .Values "favourite-service" "hpa" "maxReplicas" }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ index .Values "favourite-service" "hpa" "cpuUtilization" }}