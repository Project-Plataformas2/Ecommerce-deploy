apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: cloud-config-hpa
  namespace: {{ .Values.global.namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: cloud-config
  minReplicas: {{ index .Values "cloud-config" "hpa" "minReplicas" }}
  maxReplicas: {{ index .Values "cloud-config" "hpa" "maxReplicas" }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ index .Values "cloud-config" "hpa" "cpuUtilization" }}