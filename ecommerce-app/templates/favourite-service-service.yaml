apiVersion: v1
kind: Service
metadata:
  name: favourite-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: favourite-service
spec:
  selector:
    app: favourite-service
  ports:
  - name: http
    port: {{ index .Values "favourite-service" "service" "port" }}
    targetPort: {{ index .Values "favourite-service" "service" "port" }}
    protocol: TCP
  type: {{ index .Values "favourite-service" "service" "type" }}
