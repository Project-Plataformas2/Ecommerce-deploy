apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: product-service
spec:
  selector:
    app: product-service
  ports:
  - name: http
    port: {{ index .Values "product-service" "service" "port" }}
    targetPort: {{ index .Values "product-service" "service" "port" }}
    protocol: TCP
  type: {{ index .Values "product-service" "service" "type" }}
